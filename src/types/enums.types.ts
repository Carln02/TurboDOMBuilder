/**
 * @group Types
 * @category Enums
 */
enum Direction {
    vertical = "vertical",
    horizontal = "horizontal",
}

/**
 * @group Types
 * @category Enums
 */
enum SideH {
    left = "left",
    right = "right",
}

/**
 * @group Types
 * @category Enums
 */
enum SideV {
    top = "top",
    bottom = "bottom",
}

/**
 * @group Types
 * @category Enums
 */
enum Side {
    top = "top",
    bottom = "bottom",
    left = "left",
    right = "right",
}

/**
 * @group Types
 * @category Enums
 */
enum InOut {
    in = "in",
    out = "out",
}

/**
 * @group Types
 * @category Enums
 */
enum OnOff {
    on = "on",
    off = "off"
}

/**
 * @group Types
 * @category Enums
 */
enum Open {
    open = "open",
    closed = "closed"
}

/**
 * @group Types
 * @category Enums
 */
enum Shown {
    visible = "visible",
    hidden = "hidden",
}

/**
 * @group Types
 * @category Enums
 */
enum AccessLevel {
    public = "public",
    protected = "protected",
    private = "private",
}

/**
 * @group Types
 * @category Enums
 */
enum Range {
    min = "min",
    max = "max",
}

export {Direction, Side, SideV, SideH, InOut, OnOff, Open, Shown, AccessLevel, Range};