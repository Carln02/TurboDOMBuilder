enum Direction {
    vertical = "vertical",
    horizontal = "horizontal",
}

enum InOut {
    in = "in",
    out = "out",
}

enum AccessLevel {
    public = "public",
    protected = "protected",
    private = "private",
}

enum Range {
    min = "min",
    max = "max",
}

export {Direction, InOut, AccessLevel, Range};