enum Direction {
    vertical = "vertical",
    horizontal = "horizontal",
}

enum Side {
    top = "top",
    bottom = "bottom",
    left = "left",
    right = "right",
}

enum InOut {
    in = "in",
    out = "out",
}

enum OnOff {
    on = "on",
    off = "off"
}

enum Open {
    open = "open",
    closed = "closed"
}

enum Shown {
    visible = "visible",
    hidden = "hidden",
}

enum AccessLevel {
    public = "public",
    protected = "protected",
    private = "private",
}

enum Range {
    min = "min",
    max = "max",
}

export {Direction, Side, InOut, OnOff, Open, Shown, AccessLevel, Range};