import {TurboProperties} from "../../core/definitions/turbo-types";

type TurboPopupProperties = TurboProperties & {
    viewportMargin?: number,
    offsetFromParent?: number,
};

export {TurboPopupProperties};